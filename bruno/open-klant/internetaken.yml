info:
  name: internetaken
  type: http
  seq: 1

http:
  method: GET
  url: "{{open-klant-baseurl}}/v1/api/internetaken"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
